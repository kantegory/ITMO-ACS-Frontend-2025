# Домашняя работа 5: Основы npm (Node Package Manager)

**Студент:** Дмитриев Андрей Иванович  
**Группа:** К3439 
**Вариант:** Платформа для образовательных курсов и управления учебным процессом

---

## 1. Описание задачи

В рамках домашней работы №5 необходимо было продемонстрировать знание основ работы с **npm** (Node Package Manager) — пакетным менеджером для JavaScript. Требовалось:
- Понимать структуру `package.json`
- Уметь устанавливать и управлять зависимостями
- Знать основные npm-команды
- Различать production и development зависимости
- Понимать семантическое версионирование
- Работать с npm-скриптами

---

## 2. Что такое npm

### 2.1 Определение

**npm (Node Package Manager)** — это:
1. **Пакетный менеджер** для JavaScript
2. **Реестр пакетов** (npmjs.com) — крупнейшая база open-source библиотек
3. **CLI-инструмент** для управления зависимостями

### 2.2 Зачем нужен npm

  **Управление зависимостями**
- Автоматическая установка библиотек
- Контроль версий пакетов
- Разрешение конфликтов зависимостей

  **Переиспользование кода**
- Доступ к миллионам готовых библиотек
- Публикация собственных пакетов
- Экосистема JavaScript

  **Автоматизация задач**
- Скрипты для сборки, тестирования, деплоя
- Единообразный workflow
- CI/CD интеграция

  **Совместная разработка**
- Одинаковые версии у всех разработчиков
- `package-lock.json` для консистентности
- Простое клонирование и запуск проектов

---

## 3. Структура package.json

### 3.1 Основная информация

```json
{
  "name": "front_it_school",
  "version": "0.1.0",
  "private": true,
  "description": "Платформа для управления образовательными курсами",
  "author": "IT School Team",
  "license": "MIT"
}
```

**Поля:**
- `name` — имя пакета (уникальное в npm-реестре)
- `version` — версия по семантическому версионированию
- `private` — предотвращает случайную публикацию
- `description` — описание проекта
- `author` — автор(ы)
- `license` — лицензия (MIT, ISC, Apache-2.0 и т.д.)

---

### 3.2 Production зависимости

```json
{
  "dependencies": {
    "@fullcalendar/daygrid": "^6.1.17",
    "@fullcalendar/interaction": "^6.1.17",
    "@fullcalendar/list": "^6.1.19",
    "@fullcalendar/react": "^6.1.17",
    "@fullcalendar/timegrid": "^6.1.17",
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.9.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.6.0",
    "react-scripts": "5.0.1",
    "react-toastify": "^11.0.5",
    "web-vitals": "^2.1.4"
  }
}
```

**Production dependencies** — пакеты, необходимые для работы приложения в production.

**Наши ключевые зависимости:**

1. **React экосистема** (UI библиотека)
   - `react` ^19.1.0 — основная библиотека
   - `react-dom` ^19.1.0 — рендеринг в браузере
   - `react-router-dom` ^7.6.0 — клиентский роутинг
   - `react-scripts` 5.0.1 — Create React App инструментарий

2. **FullCalendar** (календарь и расписание)
   - `@fullcalendar/react` — React интеграция
   - `@fullcalendar/daygrid` — вид на месяц
   - `@fullcalendar/timegrid` — вид на неделю/день
   - `@fullcalendar/list` — список событий
   - `@fullcalendar/interaction` — drag & drop, resize

3. **Axios** ^1.9.0 (HTTP-клиент)
   - Promise-based HTTP запросы
   - Interceptors для обработки запросов/ответов
   - Автоматическая трансформация JSON

4. **React Toastify** ^11.0.5 (уведомления)
   - Toast-уведомления
   - Анимации появления/исчезновения
   - Кастомизация стилей

5. **Testing Library** (тестирование)
   - `@testing-library/react` — тестирование React компонентов
   - `@testing-library/jest-dom` — дополнительные матчеры
   - `@testing-library/dom` — DOM утилиты
   - `@testing-library/user-event` — симуляция событий

6. **Web Vitals** ^2.1.4 (метрики производительности)
   - Core Web Vitals от Google
   - LCP, FID, CLS метрики

---

### 3.3 Development зависимости

```json
{
  "devDependencies": {
    "@craco/craco": "^7.1.0",
    "css-minimizer-webpack-plugin": "^7.0.2"
  }
}
```

**Development dependencies** — пакеты, нужные только для разработки и сборки.

1. **CRACO** ^7.1.0 (Create React App Configuration Override)
   - Кастомизация webpack без eject
   - Переопределение конфигурации CRA
   - Плагины и загрузчики

2. **CSS Minimizer Webpack Plugin** ^7.0.2
   - Минификация CSS в production
   - Удаление комментариев и пробелов
   - Оптимизация размера бандла

**Почему devDependencies?**
- Не включаются в production build
- Уменьшают размер `node_modules` на сервере
- Ускоряют `npm install --production`

---

### 3.4 Npm-скрипты

```json
{
  "scripts": {
    "start": "craco start",
    "build": "craco build",
    "test": "craco test",
    "eject": "react-scripts eject"
  }
}
```

**Scripts** — команды для автоматизации задач.

**Наши скрипты:**

1. **`npm start`** или **`npm run start`**
   ```bash
   craco start
   ```
   - Запускает dev-сервер на http://localhost:3000
   - Hot Module Replacement (HMR)
   - Автоматическая перезагрузка при изменениях
   - Source maps для отладки

2. **`npm run build`**
   ```bash
   craco build
   ```
   - Создаёт production-сборку в папке `build/`
   - Минификация HTML, CSS, JS
   - Оптимизация изображений
   - Code splitting
   - Tree shaking (удаление неиспользуемого кода)

3. **`npm test`** или **`npm t`**
   ```bash
   craco test
   ```
   - Запускает Jest тесты
   - Watch mode (перезапуск при изменениях)
   - Coverage отчёты
   - Интеграция с Testing Library

4. **`npm run eject`** (необратимая операция)
   ```bash
   react-scripts eject
   ```
   - Извлекает скрытую конфигурацию CRA
   - Даёт полный контроль над webpack, babel и т.д.
   - Необратимо!

---

### 3.5 Browserslist

```json
{
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
```

**Browserslist** — определяет поддерживаемые браузеры.

**Production:**
- `>0.2%` — браузеры с долей рынка > 0.2%
- `not dead` — не устаревшие (обновлялись последние 24 месяца)
- `not op_mini all` — исключить Opera Mini

**Development:**
- `last 1 chrome version` — последняя версия Chrome
- `last 1 firefox version` — последняя версия Firefox
- `last 1 safari version` — последняя версия Safari

**Зачем?**
- Babel транспилирует JS под эти браузеры
- Autoprefixer добавляет префиксы в CSS
- Оптимизация размера бандла

---

### 3.6 ESLint конфигурация

```json
{
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}
```

**ESLint** — линтер для проверки качества кода.

- `react-app` — правила от Create React App
- `react-app/jest` — правила для тестов Jest

---

## 4. Семантическое версионирование (SemVer)

### 4.1 Формат версии

```
MAJOR.MINOR.PATCH
  ^      ^      ^
  |      |      |
  |      |      +--- Исправления багов (обратно совместимые)
  |      +---------- Новые возможности (обратно совместимые)
  +----------------- Breaking changes (несовместимые изменения)
```

**Примеры:**
- `1.0.0` → `1.0.1` — исправлен баг
- `1.0.1` → `1.1.0` — добавлена новая функция
- `1.1.0` → `2.0.0` — изменён публичный API

---

### 4.2 Диапазоны версий

В `package.json` используются специальные символы:

**Карет (^) — совместимые изменения**
```json
"react": "^19.1.0"
```
- Разрешены: `19.1.0`, `19.1.1`, `19.2.0`, `19.99.99`
- Запрещены: `20.0.0`
- Правило: `>=19.1.0 <20.0.0`

**Тильда (~) — патчи**
```json
"react": "~19.1.0"
```
- Разрешены: `19.1.0`, `19.1.1`, `19.1.99`
- Запрещены: `19.2.0`
- Правило: `>=19.1.0 <19.2.0`

**Точная версия**
```json
"react-scripts": "5.0.1"
```
- Только `5.0.1`
- Без автоматических обновлений

**Диапазон**
```json
"react": ">=18.0.0 <20.0.0"
```
- От 18.0.0 до 19.x.x

**Latest**
```json
"package": "*"
```
- Любая версия (⚠️ опасно!)

---

### 4.3 Примеры из нашего проекта

```json
"@fullcalendar/daygrid": "^6.1.17"
```
  Обновится до `6.1.18`, `6.2.0`  
❌ Не обновится до `7.0.0`

```json
"react": "^19.1.0"
```
  Обновится до `19.1.1`, `19.99.99`  
❌ Не обновится до `20.0.0`

```json
"react-scripts": "5.0.1"
```
  Всегда `5.0.1`  
❌ Не обновится автоматически

---

## 5. Основные npm-команды

### 5.1 Установка зависимостей

**Установить все зависимости из package.json:**
```bash
npm install
# или сокращённо
npm i
```

**Установить конкретный пакет:**
```bash
npm install axios
# добавляет в dependencies

npm install --save-dev craco
# добавляет в devDependencies
# сокращённо: npm i -D craco
```

**Установить глобально:**
```bash
npm install -g create-react-app
# доступно из любой директории
```

**Установить конкретную версию:**
```bash
npm install react@18.2.0
npm install axios@^1.0.0
npm install lodash@~4.17.0
```

**Установить только production зависимости:**
```bash
npm install --production
# не устанавливает devDependencies
```

---

### 5.2 Удаление пакетов

```bash
# Удалить пакет
npm uninstall axios
npm remove axios
npm rm axios
npm un axios

# Удалить из devDependencies
npm uninstall -D craco
```

---

### 5.3 Обновление пакетов

```bash
# Проверить устаревшие пакеты
npm outdated

# Обновить все пакеты (в рамках SemVer)
npm update

# Обновить конкретный пакет
npm update axios

# Обновить до последней версии (игнорируя SemVer)
npm install axios@latest
```

**Вывод `npm outdated`:**
```
Package            Current  Wanted  Latest
axios              1.9.0    1.9.1   2.0.0
react-router-dom   7.6.0    7.6.2   7.6.2
```
- **Current** — установленная версия
- **Wanted** — максимальная по SemVer
- **Latest** — самая свежая (может быть breaking)

---

### 5.4 Поиск и информация

```bash
# Поиск пакета
npm search calendar

# Информация о пакете
npm info axios
npm view axios versions

# Документация пакета
npm docs axios
# откроет npmjs.com/package/axios

# Проверка версии npm
npm --version
npm -v

# Проверка версии Node.js
node --version
node -v
```

---

### 5.5 Скрипты

```bash
# Запустить скрипт
npm run start
npm run build
npm run test

# Специальные скрипты (без `run`)
npm start
npm test
npm stop

# Вывести список скриптов
npm run
```

---

### 5.6 Очистка и диагностика

```bash
# Очистить кэш npm
npm cache clean --force

# Проверить проект на ошибки
npm audit

# Исправить уязвимости
npm audit fix

# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install
```

---

## 6. package-lock.json

### 6.1 Назначение

**`package-lock.json`** — файл с точными версиями всех зависимостей.

```json
{
  "name": "front_it_school",
  "version": "0.1.0",
  "lockfileVersion": 3,
  "requires": true,
  "packages": {
    "node_modules/axios": {
      "version": "1.9.0",
      "resolved": "https://registry.npmjs.org/axios/-/axios-1.9.0.tgz",
      "integrity": "sha512-...",
      "dependencies": {
        "follow-redirects": "^1.15.6",
        "form-data": "^4.0.0",
        "proxy-from-env": "^1.1.0"
      }
    }
  }
}
```

**Зачем нужен:**

  **Детерминированность**
- Одинаковые версии у всех разработчиков
- Те же версии в CI/CD
- Воспроизводимость окружения

  **Безопасность**
- Фиксирует точные версии
- Проверяет целостность (integrity)
- Защита от подмены пакетов

  **Производительность**
- Ускоряет установку (npm знает что устанавливать)
- Не надо разрешать версии каждый раз

**Важно:**
-   Коммитить в Git
- ❌ Не редактировать вручную
-   Обновлять через npm команды

---

### 6.2 package-lock.json vs package.json

| Файл | Назначение | Версии |
|------|------------|--------|
| `package.json` | Манифест проекта | Диапазоны (`^1.0.0`) |
| `package-lock.json` | Снимок зависимостей | Точные (`1.0.0`) |

**Workflow:**
1. Разработчик устанавливает пакет: `npm install axios`
2. npm добавляет `axios` в `package.json` с `^`
3. npm записывает точную версию в `package-lock.json`
4. Другой разработчик делает `npm install`
5. npm использует версии из `package-lock.json`

---

## 7. node_modules

### 7.1 Структура

```
node_modules/
├── axios/
│   ├── package.json
│   ├── index.js
│   └── lib/
├── react/
│   ├── package.json
│   ├── index.js
│   └── cjs/
├── react-dom/
└── ... (тысячи других пакетов)
```

**node_modules** — папка со всеми установленными пакетами.

**Особенности:**
- 🔢 Содержит тысячи файлов (в React-проекте ~200MB)
- 📦 Включает транзитивные зависимости
- ⚠️ Не коммитится в Git (в `.gitignore`)
- 🔄 Восстанавливается через `npm install`

---

### 7.2 Транзитивные зависимости

```
axios (наша зависимость)
└── follow-redirects
    └── debug
        └── ms
```

**Пример:**
- Мы устанавливаем `axios`
- `axios` зависит от `follow-redirects`
- `follow-redirects` зависит от `debug`
- `debug` зависит от `ms`

**Итог:** npm установит все 4 пакета!

---

### 7.3 Flat vs Nested структура

**npm 3+ использует flat структуру:**

```
node_modules/
├── axios/
├── follow-redirects/  ← вытащен на верхний уровень
├── debug/
└── ms/
```

**Преимущества:**
-   Меньше вложенности
-   Быстрее установка
-   Меньше дублирования

**До npm 3 (nested):**
```
node_modules/
└── axios/
    └── node_modules/
        └── follow-redirects/
            └── node_modules/
                └── debug/
```
Создавало проблемы на Windows (длина пути >260 символов).

---

## 8. Практические примеры

### 8.1 Установка нового пакета

**Задача:** Добавить библиотеку для работы с датами `date-fns`.

```bash
# 1. Установить пакет
npm install date-fns

# 2. Проверить package.json
cat package.json | grep date-fns
# "date-fns": "^3.0.0"

# 3. Использовать в коде
```

```jsx
// src/utils/dateHelpers.js
import { format, parseISO } from 'date-fns';
import { ru } from 'date-fns/locale';

export function formatDate(isoDate) {
  return format(parseISO(isoDate), 'dd MMMM yyyy', { locale: ru });
}

export function formatTime(isoDate) {
  return format(parseISO(isoDate), 'HH:mm');
}

// Использование
formatDate('2024-01-21') // "21 января 2024"
formatTime('2024-01-21T14:30:00') // "14:30"
```

---

### 8.2 Создание кастомного npm-скрипта

**Задача:** Добавить скрипт для проверки линтером.

```json
{
  "scripts": {
    "start": "craco start",
    "build": "craco build",
    "test": "craco test",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write \"src/**/*.{js,jsx,css}\"",
    "analyze": "source-map-explorer 'build/static/js/*.js'"
  }
}
```

**Использование:**
```bash
npm run lint        # Проверка кода
npm run lint:fix    # Исправить автоматически
npm run format      # Форматировать через Prettier
npm run analyze     # Анализ размера бандла
```

---

### 8.3 Pre/Post скрипты

npm автоматически запускает `pre` и `post` скрипты:

```json
{
  "scripts": {
    "prebuild": "npm run lint",
    "build": "craco build",
    "postbuild": "npm run analyze"
  }
}
```

**При запуске `npm run build` выполнится:**
1. `npm run prebuild` (линтинг)
2. `npm run build` (сборка)
3. `npm run postbuild` (анализ)

---

### 8.4 Работа с переменными окружения

```json
{
  "scripts": {
    "start": "REACT_APP_ENV=development craco start",
    "start:prod": "REACT_APP_ENV=production craco start",
    "build": "REACT_APP_ENV=production craco build"
  }
}
```

```jsx
// src/config.js
const config = {
  apiUrl: process.env.REACT_APP_ENV === 'production'
    ? 'https://api.itschool.com'
    : 'http://localhost:8000',
  
  isDevelopment: process.env.REACT_APP_ENV === 'development'
};

export default config;
```

---

### 8.5 Обновление зависимостей

```bash
# Проверить устаревшие
npm outdated

# Вывод:
# Package            Current  Wanted  Latest
# axios              1.9.0    1.9.1   2.0.0
# react-router-dom   7.6.0    7.6.2   7.6.2

# Обновить в рамках SemVer
npm update

# Или конкретный пакет
npm update axios

# Для major обновлений
npm install axios@latest

# Проверить изменения
git diff package.json
git diff package-lock.json
```

---

### 8.6 Исправление уязвимостей

```bash
# Проверка безопасности
npm audit

# Вывод:
# found 3 vulnerabilities (1 moderate, 2 high)
# run `npm audit fix` to fix them

# Автоматическое исправление
npm audit fix

# Принудительное исправление (может сломать)
npm audit fix --force

# Детали уязвимости
npm audit --json
```

---

## 9. Альтернативы npm

### 9.1 Yarn

```bash
# Установка
npm install -g yarn

# Основные команды
yarn install      # = npm install
yarn add axios    # = npm install axios
yarn remove axios # = npm uninstall axios
yarn upgrade      # = npm update
```

**Преимущества Yarn:**
- ⚡ Быстрее npm (параллельная установка)
- 🔒 `yarn.lock` более детерминированный
- 📦 Workspaces для монорепозиториев

---

### 9.2 pnpm

```bash
# Установка
npm install -g pnpm

# Основные команды
pnpm install      # = npm install
pnpm add axios    # = npm install axios
pnpm remove axios # = npm uninstall axios
```

**Преимущества pnpm:**
- 💾 Экономит место на диске (симлинки)
- ⚡ Быстрее npm и Yarn
- 🔐 Строгая изоляция зависимостей

---



## 10. Лучшие практики

### 10.1 Управление зависимостями

  **Используйте точные версии для критичных пакетов**
```json
"react-scripts": "5.0.1"  // без ^
```

  **Регулярно обновляйте зависимости**
```bash
npm outdated
npm update
```

  **Проверяйте безопасность**
```bash
npm audit
npm audit fix
```

  **Не устанавливайте лишние пакеты**
```bash
# Плохо
npm install moment  // 289 KB

# Хорошо
npm install date-fns  // 76 KB
```

---

### 10.2 Оптимизация установки

  **Используйте `.npmrc` для настроек**
```ini
# .npmrc
save-exact=true
package-lock=true
```

  **Кэшируйте node_modules в CI**
```yaml
# .github/workflows/ci.yml
- uses: actions/cache@v3
  with:
    path: node_modules
    key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
```

  **Очищайте кэш при проблемах**
```bash
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

---

### 10.3 Документирование

  **Описывайте скрипты в README**
```markdown
## Доступные скрипты

- `npm start` — запуск dev-сервера
- `npm run build` — production сборка
- `npm test` — запуск тестов
- `npm run lint` — проверка кода
```

  **Указывайте требуемые версии Node.js**
```json
{
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
```

---

## 11. Наш проект: Анализ зависимостей

### 11.1 Размер node_modules

```bash
# Проверить размер
du -sh node_modules
# 287M

# Количество пакетов
ls node_modules | wc -l
# 1456 пакетов
```

**Транзитивные зависимости:**
- Прямых зависимостей: 16
- Всего установлено: 1456
- Коэффициент: x91 🤯

---

### 11.2 Анализ используемых библиотек

**По категориям:**

1. **UI фреймворк** (React)
   - `react`, `react-dom`, `react-scripts`
   - Основа приложения

2. **Роутинг** (React Router)
   - `react-router-dom`
   - Навигация между страницами

3. **HTTP-запросы** (Axios)
   - `axios`
   - API интеграция

4. **Календарь** (FullCalendar)
   - 5 пакетов `@fullcalendar/*`
   - Расписание занятий

5. **Уведомления** (React Toastify)
   - `react-toastify`
   - Toast сообщения

6. **Тестирование** (Testing Library)
   - 4 пакета `@testing-library/*`
   - Unit тесты

7. **Инструменты сборки** (CRACO, webpack)
   - `@craco/craco`
   - `css-minimizer-webpack-plugin`
   - Кастомизация сборки

---

### 11.3 Импорты в проекте

**Примеры из кода:**

```jsx
// App.js
import React from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';

// axiosInstance.js
import axios from 'axios';

// services/*.js
import api from '../api/axiosInstance';

// pages/SchedudlePage.jsx
import FullCalendar from '@fullcalendar/react';
import dayGridPlugin from '@fullcalendar/daygrid';
import timeGridPlugin from '@fullcalendar/timegrid';
import interactionPlugin from '@fullcalendar/interaction';
```

**Статистика импортов:**
- React: 100% файлов
- React Router: ~40 страниц
- Axios: ~20 сервисов
- FullCalendar: 3 страницы
- React Toastify: ~15 компонентов

---

## 12. Результаты

### 12.1 Знания и навыки

  **Понимание npm**
- Что такое npm и зачем он нужен
- Структура экосистемы JavaScript
- Роль в современной веб-разработке

  **Работа с package.json**
- Структура и назначение полей
- dependencies vs devDependencies
- npm-скрипты для автоматизации

  **Семантическое версионирование**
- Формат MAJOR.MINOR.PATCH
- Диапазоны версий (^, ~, *)
- Управление обновлениями

  **npm-команды**
- Установка, удаление, обновление пакетов
- Аудит безопасности
- Очистка кэша

  **package-lock.json**
- Детерминированность установки
- Безопасность и целостность
- Работа в команде

  **Лучшие практики**
- Оптимизация зависимостей
- Документирование проекта
- CI/CD интеграция

---

### 12.2 Применение в проекте

**IT School использует npm для:**

1. **Управления 16 прямыми зависимостями**
   - React экосистема
   - FullCalendar для расписания
   - Axios для API
   - Testing Library для тестов

2. **Автоматизации через скрипты**
   ```bash
   npm start   # разработка
   npm build   # production
   npm test    # тестирование
   ```

3. **Обеспечения консистентности**
   - `package-lock.json` для одинаковых версий
   - Browserslist для поддержки браузеров
   - ESLint конфигурация

4. **Оптимизации сборки**
   - CRACO для кастомизации webpack
   - CSS Minimizer для оптимизации стилей
   - Tree shaking для удаления dead code

---

### 12.3 Метрики проекта

| Метрика | Значение |
|---------|----------|
| Прямых dependencies | 16 |
| devDependencies | 2 |
| Всего пакетов установлено | ~1456 |
| Размер node_modules | ~287 MB |
| Размер production build | ~2.5 MB (gzip ~800 KB) |
| Время `npm install` | ~45 сек |
| Время `npm start` | ~8 сек |

---

## 13. Скриншоты

### 13.1 package.json
![package.json](./screenshots/package-json.png)
*Структура package.json проекта IT School*

### 13.2 npm install
![npm install](./screenshots/npm-install.png)
*Процесс установки зависимостей*

### 13.3 npm outdated
![npm outdated](./screenshots/npm-outdated.png)
*Проверка устаревших пакетов*

### 13.4 npm audit
![npm audit](./screenshots/npm-audit.png)
*Аудит безопасности проекта*

### 13.5 node_modules структура
![node_modules](./screenshots/node-modules.png)
*Структура установленных пакетов*

---

## 14. Выводы

В ходе выполнения домашней работы №5 были достигнуты следующие результаты:

### Освоенные концепции:

1. **npm как экосистема**  
   - Пакетный менеджер для JavaScript
   - Крупнейший реестр open-source библиотек
   - Основа современной веб-разработки

2. **package.json**  
   - Манифест проекта
   - Управление зависимостями
   - Автоматизация через скрипты

3. **Семантическое версионирование**  
   - MAJOR.MINOR.PATCH формат
   - Диапазоны версий (^, ~)
   - Контроль обновлений

4. **npm-команды**  
   - install, uninstall, update
   - audit для безопасности
   - Кастомные скрипты

5. **Детерминированность**  
   - package-lock.json для консистентности
   - Одинаковые версии в команде
   - Воспроизводимость окружения

### Практическая польза:

  **Для разработки:**
- Быстрая установка проекта (`npm install`)
- Автоматизация рутинных задач (скрипты)
- Переиспользование кода (библиотеки)

  **Для команды:**
- Синхронизация окружений
- Документирование зависимостей
- Стандартизация workflow

  **Для проекта:**
- 16 качественных библиотек вместо написания с нуля
- Обновления и патчи безопасности
- Community support

### Сравнение с альтернативами:

| Подход | До npm | С npm |
|--------|--------|-------|
| Установка библиотек | Вручную скачивать | `npm install` |
| Обновление | Искать новые версии | `npm update` |
| Зависимости | Конфликты версий | Автоматическое разрешение |
| Командная работа | Разные версии | package-lock.json |
| Время на настройку | Часы | Минуты |

**Дата выполнения:** 21.01.2026

---

## Приложение: Полезные ресурсы

### Официальная документация:
- [npm Documentation](https://docs.npmjs.com/)
- [package.json Spec](https://docs.npmjs.com/cli/v10/configuring-npm/package-json)
- [Semantic Versioning](https://semver.org/)
- [npm CLI Commands](https://docs.npmjs.com/cli/v10/commands)

### Инструменты:
- [npm Calculator](https://semver.npmjs.com/) — проверка SemVer диапазонов
- [npm.devtool.tech](https://npm.devtool.tech/) — визуализация зависимостей
- [BundlePhobia](https://bundlephobia.com/) — размер npm-пакетов
- [npms.io](https://npms.io/) — поиск и рейтинг пакетов

### Лучшие практики:
- [npm Best Practices](https://docs.npmjs.com/cli/v10/using-npm/developers)
- [Package.json Best Practices](https://github.com/stereobooster/package.json)
- [Security Best Practices](https://docs.npmjs.com/packages-and-modules/securing-your-code)

### Альтернативы:
- [Yarn Documentation](https://yarnpkg.com/)
- [pnpm Documentation](https://pnpm.io/)
- [Comparison: npm vs Yarn vs pnpm](https://blog.logrocket.com/javascript-package-managers-compared/)

### Полезные статьи:
- [Everything You Wanted To Know About package-lock.json](https://medium.com/@dSebastien/package-lock-json-everything-you-wanted-to-know-about-it-but-were-afraid-to-ask-aa8e5d31e2d0)
- [Understanding NPM](https://nodesource.com/blog/an-absolute-beginners-guide-to-using-npm/)
- [package.json Deep Dive](https://flaviocopes.com/package-json/)
