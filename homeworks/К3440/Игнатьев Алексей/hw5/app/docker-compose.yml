version: "3.9"
services:
  frontend:
    build: .
    container_name: crypto-bank-admin
    ports:
      - "46346:3000"
    environment:
      # You can override this to point to your backend
      - API_BASE=${API_BASE:-http://192.168.254.121:8000}
      - NEXT_PUBLIC_API_BASE=${NEXT_PUBLIC_API_BASE:-http://192.168.254.121:8000}
      - NODE_ENV=production
    # If your backend is also in compose, you can declare a depends_on
    # depends_on:
    #   - backend
    # networks:
    #   - appnet
#  backend:
#    image: your-backend-image
#    ports:
#      - "8080:8080"
#    networks:
#      - appnet
#networks:
#  appnet:
#    driver: bridge
